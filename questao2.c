#include <stdio.h>
#include <string.h>

  	int(char string[50]){
	  for(i=0;i<n;i++);
	  
}

int main(){
	char string;
	
	printf("Digite uma palavra:");
	gets("%d", &string);
	
	printf("Assim fica a palavra %d ao contrário:");
	scanf("%d", sizeof(string)strlen);
	
}
