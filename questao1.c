#include <stdio.h>

	int (float numeros[], int media_aritmetica){
		for(i=0; i<numeros;i++);
	}
	
int main(){
	float numeros;
	int media_aritmetica;
	
	printf("Digite alguns números:");
	gets("%d", &numeros);
	
	printf("Calcule a média aritmética entre esses números:");
	scanf("%d", &media_aritmetica(numeros)stdin);
	
}
