#include <stdio.h>

   int i(int num1, int num2, int fatorial){
   		for(i =0; i<n;i++);	
   }

int main(){
	
	int num1;
	int num2;
	int fatorial=0;
	
	printf("Digite o primeiro número:");
	gets("%d", &num1);
	
	printf("Digite o segundo número:");
	gets("%d", &num2);
	
	print("Esse é o fatorial entre os números:");
	scanf("%d", &fatorial(num1, num2)stdin);
	
}
